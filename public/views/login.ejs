<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/styles.css">
    <title>Login</title>
</head>
<body id="login-screen">
    <form action="/login" method="POST">
        <div id="error">
            <% if (messages.error) { %>
                <%= messages.error %>
                <% } %>
        </div>
        <div id="login-inputs">
            <label class="login-label">Email</label>
            <input class="login" type="email" name="email" required/>
            <label class="login-label">Password</label>
            <input class="login" type="password" name="password" required/>
            <button type="submit">Enter</button>
            <button type="button" onclick="location.href='/register';">Register</button>
            <a id="comp-app" href="https://github.com/Living-Tribunal/Starbound-Conquest-Companion-App">Companion App</a>
        </div>
    </form>
    
</body>
</html>
